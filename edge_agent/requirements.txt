# Station-OS Edge Agent — Python dependencies
# Install with: pip install -r requirements.txt

# File system monitoring (cross-platform, optimized for Windows)
watchdog==4.0.1

# Async HTTP client for Supabase REST API
httpx==0.27.0

# Data validation and serialization
pydantic==2.7.1
pydantic-settings==2.3.0

# Environment variables (.env file support)
python-dotenv==1.0.1

# YAML config file parsing
PyYAML==6.0.1

# Windows Service support (Windows only — optional for dev on Linux/Mac)
pywin32==306; sys_platform == "win32"

# Decimal precision for financial calculations
# (built-in Python decimal module — no extra install needed)

# Logging with structured output
structlog==24.2.0

# Retry logic for network calls
tenacity==8.3.0
